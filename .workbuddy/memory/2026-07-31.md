# 2026-07-31

## TestPrepPilot 考试页重设计（Ardot 画布）

- 需求：affiliate 站每个考试页（[state]/[credential].astro 模板）重做排版/视觉，保留原有内容结构，增强专业感与清晰度。
- 设计方向：NYT 编辑级（editorial-minimalist-archive 骨架 + Editorial NYT Cooking 配色 #326891 新闻蓝 / #C7593E 评价橙 + Source Serif 4 衬线标题 + 发丝线分隔，去掉粗边框与硬投影）。文件类型 `.ardot`，场景 网站设计。
- 设计文件已建：`TestPrepPilot — 考试页重设计`（fileId 709768684347649，url https://ardot.tencent.com/file/709768684347649）。
- 已完成的骨架（Batch 1 成功）：图标组件库 box `2:2`（check `2:3`、star `2:6`、chevron-down `2:9`、arrow-left `2:12`、arrow-up-right `2:15`）；页面 frame `2:18`（1440 宽）；Header `2:19`、Header Rule `2:20`、Hero `2:21`、Content `2:22`、Footer `2:23` 容器均为空。
- 待续（连接恢复后填充）：Header 内容 / Hero 文案+事实条 / Sidebar+正文(Requirements/Providers+Featured+Table/Route/FAQ) / Footer。示例内容取自 `credentials.ts` 德州房地产执照（TX Real Estate Sales Agent License）。
- 真实数据字段已读取：providers(Colibri $459 4.6 / Kaplan $399 4.5 / AceableAgent $495 4.8)、steps×5、faq×3、requirements(Background check / Fingerprinting $38.25 / Sponsoring broker)、fact(180h / $43 / $205 / TREC)。
- 阻塞：执行 Header 填充时发生 `NO_ADAPTER`，等待 3 次重试后仍失败，已按规则停止。resume 时直接对 `2:19/2:21/2:22/2:23` 继续插入即可。

## 批量重做其余考试页（数据驱动生成器）
- 站点 `src/data/credentials.ts` 共 **8 个考试**：TX/CA/FL Real Estate、Enrolled Agent、OSHA 10/30、EPA 608、CPA。用户要求"先做一半"。TX 已作为模板完成。本次做 4 个：**california / florida / enrolled-agent / osha-10-construction**（地产州照+税务全国证+安全全国证，覆盖均衡）。剩 OSHA 30、EPA 608、CPA 下次。
- 写了生成器 `design-output/build-exam-pages.cjs`（CommonJS，用托管 node 22 运行）：读取 credentials.ts → 转 JS（strip import + `export const credentials:` 改 `module.exports=`，并在首个 `export function` 处截断，避免底部 TS helper 报错；临时文件用 `.cjs` 后缀以绕过项目 `"type":"module"`；`fs.unlinkSync` 需 try/catch，因运行时 safe-delete shim 拦截会抛错）→ 用同一套 NYT 编辑级 CSS 按 Texas 模板结构产出各页。
- 每个考试都 WebSearch 核实了考试内容细分：CA 7 大科目权重（DRE RE425）、FL 19 考点权重（DBPR）、EA 三部分子 topic（PSI 2026 接手 Prometric，$317/part，500/800 通过）、OSHA 10 必修模块。FAQ 补到 11–13 条/页。
- 产出：`exam-california-real-estate-license.html`(47KB)、`exam-florida-real-estate-license.html`(45KB)、`exam-enrolled-agent.html`(47KB)、`exam-osha-10-construction.html`(44KB)。校验：无 `undefined`、无残留 `${`、9 个 section id 齐全。
- 生成器可复用：往 `EXTRAS` 加 slug 条目即可再生成 OSHA30/EPA608/CPA。注意 credentials.ts 底部有 NOTE 称曾注入 15 个 mock 凭证已于 2026-07-31 移除。

## 续：用户放弃 ardot 画布，直接要代码
- 画布适配器长时间 `NO_ADAPTER` 不可用，用户明确要求不再用 ardot，直接产出可运行应用。
- 交付：standalone HTML 应用 `design-output/exam-page-redesign.html`。将规划好的 NYT 编辑级设计（#326891 新闻蓝 / #C7593E 评价橙 / Source Serif 4 衬线标题 / Inter 正文 / 发丝线 / 大留白 / 圆角 4px）用真实德州房地产执照数据 + 内联 CSS/JS 严格实现。
- 包含：sticky TOC 滚动高亮（IntersectionObserver）、FAQ 手风琴、响应式（≤980px 侧栏隐藏、事实条与表格堆叠）。
- 全程未再调用任何 ardot 工具。若日后画布恢复，仍可按 `2:19/2:21/2:22/2:23` 节点续接原 ardot 设计稿。

## 续2：用户要求内容厚实度对标 cnbecker EA 页
- 用户认可视觉风格，但初版内容太薄，要求按 `https://cnbecker.com/resources/ea-registration-flow/` 的翔实结构重写德州房产执照页。
- 二次 WebSearch 核实关键事实：德州当前为 6×30h=180h 六门必修课（Principles I/II、Law of Agency、Law of Contracts、Promulgated Contract Forms、Real Estate Finance）；考试 125 题（85 全国 + 40 州），240 分钟，每部分独立 70%（56/85、21/40），$43/次，Pearson VUE 现场机考；首次通过率 ~56%（2025）；总费用 ~$570–$1,290；申请费近期在 $185–$206 浮动（已做区间+核实提示，符合 affiliate 站准确性责任）。
- 重写 `design-output/exam-page-redesign.html`，结构对标 cnbecker：事实条 → Quick Answer(含核心优势) → 五段路线图谱(path-flow) → 资格与要求(六门课程表 + 销售员vs经纪人对比) → 考试详解(结构表+全国/州话题双栏+考试日要点表) → 批准机构(赞助精选+对比表) → 六步详细路线(每步含 tip/note callout) → 时间&费用双表 → 11 条 FAQ → 摘要+CTA。
- 复用已批准 NYT 编辑级 token（#326891 / #C7593E / Source Serif 4 + Inter / 4px 圆角 / 发丝线 / 大留白），新增 quick-answer、path-flow、callout、topic-grid、summary-box、cta-band 组件样式。

## 续3：完成剩余 3 个考试（OSHA 30 / EPA 608 / CPA），全 8 个收尾
- 用户要求"继续高标准完成整个任务"。读取 `credentials.ts` 确认 OSHA30/EPA608/CPA 三个 slug 存在且字段齐全（quickAnswer/roadmap/registrationSteps/comparison/timeline/costBreakdown/summaryPoints 均有）。
- 为这 3 个 slug 在 `build-exam-pages.cjs` 的 `EXTRAS` 补内容（已 WebSearch 核实）：OSHA 30 = 必修~14–15h（Intro to OSHA、Managing Safety & Health、Focus Four 6h、PPE、Health Hazards、Stairways/Ladders）+ 12h elective + 4h additional；EPA 608 = Core + Type I/II/III/Universal，每节 25 题 70%（18/25），永久不失效，新测 A2L（R-454B/R-32）；CPA = 3 Core(AUD/FAR/REG)+1 Discipline(BAR/ISC/TCP)，4h/节，75/99 通过，150 学分，~$262.64/节，18–30 月窗口。
- 生成器质量修复（影响全部 7 个 generated 页）：① `hFaq` 统一 `{q,a}` 与 `{question,answer}` 两种 shape（原生 c.faq 用后者，否则 EA/CPA 会出空 FAQ 行）；② FAQ 标题与 TOC 标签对 national 用 "Frequently Asked Questions"，state 用 "State FAQ"（之前 TOC 硬编码 State FAQ，national 页不一致）；③ 去掉 CPA 页与原生 c.faq 重复的 "What is CPA Evolution?" 问题。
- 重新运行：生成 7 页（CA/FL/EA/OSHA10 重建 + OSHA30/EPA608/CPA 新建，TX 模板 `exam-page-redesign.html` 不动）。文件：`exam-osha-30-construction.html`(47KB)、`exam-epa-608-certification.html`(50KB)、`exam-cpa-exam.html`(54KB)。
- 校验：3 个新页均无 `undefined`、无残留 `${`，9 个 section id 齐全；national 页标题/TOC 均为 "Frequently Asked Questions"，CA/FL 为 "State FAQ"；CPA 仅 1 条 Evolution 问题（其余在答案正文提及）。全 8 考试（TX 模板 + 7 generated）已齐。

## 续4：重做 "The Path at a Glance" 段落（用户反馈丑）
- 根因 bug：`hPath` 原本在圆圈里渲染 `label.charAt(0)`（首字母，如 C/P/R）且多一个空的 `<div class="path-num">`，不是步骤序号 → 视觉崩坏。
- 重做：改为 `idx+1` 编号圆圈（serif 数字，蓝 #326891 填充，白 halo `box-shadow:0 0 0 5px var(--bg)`），节点间用 `::before` 发丝线连接（top:22px 对齐圆心），末步圆圈改橙色 #C7593E 作"终点"强调；移动端（≤980px）转竖向列表、隐藏连接线、加 hairline 分隔。
- 校验：`node -e` 统计各页 path-num=1..n 正确（OSHA30=5、EPA608=5、CPA=7、CA=6），无字母节点、无空圆圈。全 7 页已重新生成。

## 续5：把内容接入 Astro 构建，外围风格对齐首页（用户核心诉求）
- 关键发现：`design-output/exam-*.html` 是站外 standalone 文件，`npm run dev` 根本不渲染它们 —— 用户看到的是 `[state]/[credential].astro` 与 `paths/[slug].astro` 旧模板（brutalist 偏移阴影 + 硬边框），而 standalone 页用的是 NYT 新闻蓝/橙/衬线，与首页风格都不一致。
- 用户明确要求：① 把整个网站同步好（dev 能看到新页）；② 外围风格对齐首页，内容排版（9 段编辑级结构）保留。
- 首页设计语言：软现代编辑风 —— `--primary:#1d4ed8` 蓝、`--cream:#f0eee5`、`--font-heading:Outfit`/`--font-body:Inter`/`--font-mono:JetBrains Mono`、`--radius:12px`/`--radius-lg:24px`、soft shadow（无偏移阴影）、glass 卡片、mono 大写 eyebrow、`.button`/`.eyebrow`/`.provider-table`/`.affiliate-box`/`.faq-accordion` 等全局组件。
- 做法：抽 `examEditorial.mjs`（`design-output/extract-extras.mjs` 从 generator 的 EXTRAS 生成，落 `src/data/examEditorial.mjs`，8 个 slug 全含）。新建 `src/components/ExamGuide.astro`：用首页 token/组件渲染 9 段编辑级内容（hero 事实条→Overview→Quick Answer→Path 编号流→Requirements+对比表→Exam meta+topics/parts/modules+essentials→Providers→Route→Timeline→Costs→FAQ→Summary→Sources），软风格（蓝/奶油/圆角/soft shadow，末步圆圈深色收尾）。两个 route 模板改为薄包装调 ExamGuide（state 版传 stateInfo/stateData，national 版含 RelatedPaths）。复用 BreadcrumbNav/ProviderTable/AffiliateBox/FAQAccordion/InlineDisclosure/SchemaMarkup。
- 构建：`astro build`（跳过 pagefind）通过，77 页全建，8 个 `/paths/*` + 州路由（california/florida/texas）均生成。校验：grep 无真实 undefined（仅主题脚本 `typeof localStorage !== 'undefined'` 误报）；state 页 FAQ 标题=State FAQ，national=Frequently Asked Questions；eg 类齐全；CPA 事实条/7 步 Path/对比表/Exam meta/parts/essentials 全部正确渲染。
- `npm run dev` 已在 :4321 启动供预览。注意：CF Pages 若跑 `astro build`，这些动态路由页会被正常构建进 dist；standalone `design-output/*.html` 仅为本地预览留存，非线上来源。
- 结论：现在 `npm run dev` 看到的即是与首页同款的考试页，可直接 git push 触发 CF 构建。

## 续6：修复字体丑 + 州级页顶部 H1 冗余
- 用户反馈：①字体太丑；②网页顶部设计有问题。
- 字体根因：`src/styles/reset.css` 的 `@import` 只拉了 DM Sans / Playfair Display / DM Mono，但 tokens 实际要 Outfit(heading)/Inter(body)/JetBrains Mono(mono) —— 三个目标字体从未被加载，所有标题静默回退到 Playfair 衬线，正文回退 DM Sans，整体显"丑"。首页与考试页共此 bug（同源 global CSS）。
- 修复：`reset.css` 的 @import 改为同时加载 Outfit+Inter+JetBrains Mono（+保留 DM/Playfair 作 fallback）；`BaseLayout.astro` head 加 `preconnect` 到 fonts.googleapis/gstatic 提速。
- 顶部 H1 冗余：state 路由把 title 拼成 `${credential.name} in ${stateInfo.name}`，而 credential.name 已含州名（如 "California Real Estate Salesperson License in California"）。改为 `title = credential.name`（州名仍由 breadcrumb + eyebrow 体现）。已验证 dist H1 仅剩干净州名。
- 顶部视觉打磨：`ExamGuide.astro` 内 `.eg-hero` 改 eyebrow 为 primary 蓝、h1 加 weight:700 / 更紧 letter-spacing / clamp(2.4rem,5.5vw,3.6rem)，与首页 hero 排版一致。
- 构建：`astro build` 通过；dist CSS 已含 Outfit/Inter/JetBrains 引用；dev :4321 重启供预览。

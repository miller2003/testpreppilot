# 2026-08-20 顶部导航彻底重构完成

## 改动概述
按用户要求,把顶部导航栏从「Guides · How it works · Reviews · About▾(下拉) · Contact · Exams」彻底精简为 **Guides · About · Exams**(无下拉)。

## 具体变更
1. **`src/components/Header.astro`** 整体重写:移除下拉菜单 JS/CSS 钩子、移除 How it works / Reviews / Contact 三项,只保留 Guides / About / Exams(button)。
2. **`src/pages/about.astro`** 升级为 E-E-A-T 大本营:保留 hero/masthead/standards/FAQ,新增 reader testimonials(`#reviews`)、Reach the desk 联系区块(含表单,`#contact`)、深耕页 anchor nav(链到 `/methodology`、`/reviewers`、`/editorial-policy`、`/disclosure`)。
3. **`/how-it-works` `/reviews` `/contact`** 三个页面改为 meta-refresh + canonical + JS redirect 三重保险重定向到 `/about`(对应锚点:`/about`、`/about#reviews`、`/about#contact`)。
4. **`src/components/Footer.astro`** 三栏合并:删除 How it works / Reviews / Contact 三项,深页(methodology/reviewers/editorial-policy/disclosure)从 Footer 直接链。
5. **`src/data/editorial.ts`** `TRUST_PAGES` 常量删除 `contact`、`howItWorks` 两键(其余保留),所有内部调用已就地改写到 `/about#contact` 等 anchor。
6. **`src/data/guides.ts` 与 `src/pages/guides.astro`** 的 "How TestPrepPilot works" 入口删除。
7. **`src/pages/disclosure.astro / methodology.astro / editorial-policy.astro / reviewers.astro / privacy.astro`** 内部 "contact page" 链接改写为 `/about#contact`。
8. **`src/styles/layout.css`** 删除约 110 行死 CSS:`.nav-item`、`.dropdown-toggle`、`.caret`、`.dropdown-panel`、`.dropdown-link`(桌面端)和 `.mobile-parent`、`.mobile-sub`(移动端)整块移除。
9. **`astro.config.mjs`** sitemap:从 trust-page 优先级规则中删除 `how-it-works|reviews`,`filter()` 增补排除 `/how-it-works`、`/reviews`、`/contact`(同 `/affiliate-disclosure`、`/privacy-policy` 模式,canonicalised URL 不进 sitemap)。

## 验证结果(基于 `dist/` 真实产物)
- 桌面 + 移动端顶栏:`Guides`、`About`、`Exams`(button)。无下拉。
- `/about` 包含 `id="who"`、`id="reviews"`、`id="contact"` 三个锚点。
- `dist/reviews/index.html`:`content="0; url=/about#reviews"`;`dist/contact/index.html`:`content="0; url=/about#contact"`;`dist/how-it-works/index.html`:`content="0; url=/about"`。
- `dist/sitemap-0.xml` 不含 reviews / how-it-works / contact 任何 URL。
- 构建产出:147 页,Complete!

## 约定(已遵守)
- 顶栏三栏约定已改,深页(methodology/reviewers/editorial-policy/disclosure)作为 `/about` 内 anchor 链接的深耕目标保留,仍可独立访问。
- Header.astro 的 dropdown JS/CSS 钩子已彻底清理,layout.css 中对应死代码已删。

## 追加:顶栏再加 States + 全局搜索弹层(同日)
用户反馈三项顶栏太空。摸清后站点实际有 3 个产品 hub(`/exams` / `/states` / `/explore`)但只露了 `/exams`。决定加:
1. **States** — `/states` 是真实页(50 州 + DC,地理轴,与 /exams 职业轴不重复),直接加进顶栏。
2. **搜索图标弹层** — 复用 Pagefind(已用于 /explore 的 SearchUI),但在 Header 用独立 `#nav-search` mount 避免与 /explore 的 `#search` 冲突。Ctrl/Cmd+K 打开、Esc 关闭、点击 backdrop 关闭。SearchUI.astro 组件未改动。
- `/explore` **未** 加进顶栏:与 `/exams` 语义重复(都是全凭证目录),待日后确认 lens 是否不同再决定。
- 改动文件:`src/components/Header.astro`(整体重写,加 States 链接 + 两个 search-icon button + 弹层 markup + 弹层 JS)、`src/styles/layout.css`(新增 `.nav-icon-btn` / `.mobile-search-btn` / `.search-modal*` 样式块 + 把 `@media (max-width:900px) .mobile-menu-wrapper` 从 `display:block` 改成 `display:flex` 避免搜索按钮和汉堡菜单纵向堆叠)。
- 构建关键点:`package.json` 的 `build` 脚本是 `astro build && pagefind --site dist`——直接调用 `./node_modules/.bin/astro build` **不会** 生成 pagefind 索引。要么跑 `npm run build`,要么在 astro build 后手动 `./node_modules/.bin/pagefind --site dist`。本次漏跑了一次,补跑后索引到 145 页 / 11156 词。
- 最终验证(基于 `dist/` + `localhost:4321` 实测):桌面顶栏 = `Guides · States · About · [search icon] · [theme] · Exams(button)`;`#search-toggle` / `#search-toggle-mobile` / `#search-modal` / `#nav-search` 四个 ID 全部存在;`/pagefind/pagefind-ui.js` / `pagefind-ui.css` / `pagefind.js` 三项资产 HTTP 200。


